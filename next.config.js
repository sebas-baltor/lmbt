/** @type {import('next').NextConfig} */
const nextConfig = {
    env:{
        serverUrl:"https://rol-juego.azurewebsites.net",
        // serverUrl:"http://localhost:3000"
    }
}

module.exports = nextConfig
